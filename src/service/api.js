import { fetchPost, fetchGet, fetchPut, fetchDel } from '@/axios';
import CommonApi from './CommonApi/CommonApi'; // 

/**
 * 登录页面接口管理
 */
const api = {
    CommonApi, // 修改主要完成人完成单位接口
};
export default api;